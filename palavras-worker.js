onmessage = msg => {
  let val = msg.data[0];
  let ind = msg.data[1];
  // let txt = (ind) ? formedBy(val) : hasThe(val);
  // postMessage(txt);
};

// function getDataBase() {

// }

// function formedBy() {}
// function hasThe() {}
